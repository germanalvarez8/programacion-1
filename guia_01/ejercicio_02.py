firstNote = float(input("Inserte una nota: "))
secondNote = float(input("Otra mas: "))
thirdNote = float(input("La ultima: "))

print('Tu promedio es', round((firstNote + secondNote + thirdNote) / 3), 2)
